image: phpmyadmin/phpmyadmin:${{PMA_VERSION}}
container_name: ${{PROJECT_NAME_CONTAINER}}-phpmyadmin
networks:
  - mysql-phpmyadmin
ports:
  - ${{HOST_PMA_PORT}}:80
volumes:
  - /sessions
env_file:
  - environment.env
depends_on:
  - database